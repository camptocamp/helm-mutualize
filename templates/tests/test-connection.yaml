apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mutualized.fullname" . }}-test-connection"
  labels:
{{ include "mutualized.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mutualized.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
