{{- if .Values.ingress.enabled -}}
{{- $fullName := include "common.fullname" ( dict "root" . "service" .Values ) -}}
{{- $svcPort := .Values.print.service.port -}}
{{- $svcLogsPort := .Values.logs.service.port -}}
{{- $svcConfigPort := .Values.config.service.port -}}
{{- range $ingress_host := .Values.ingress.hosts }}
{{- range $prefix := list "print" "config" }}
---
{{- if semverCompare ">=1.19.0" ( trimPrefix "v" $.Capabilities.KubeVersion.Version ) }}
apiVersion: networking.k8s.io/v1
{{- else -}}
{{- if semverCompare ">=1.14-0" $.Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-{{ $ingress_host.name }}-{{ $prefix }}
  labels: {{ include "common.labels" ( dict "root" $ "service" $.Values ) | nindent 4 }}
    prefix: {{ $prefix }}
  {{- with $.Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
{{- if $ingress_host.tls }}
  tls:
    - hosts:
        - {{ $prefix }}.{{ $ingress_host.host }}
      secretName: {{ $ingress_host.tls.secretName }}-{{ $prefix }}
{{- end }}
  rules:
    - host: {{ $ingress_host.host }}
      http:
        paths:
        {{- range $.Values.ingress.paths }}
        {{- if eq $prefix "print" }}
          - path: {{ . }}
            {{- if semverCompare ">=1.14-0" $.Capabilities.KubeVersion.GitVersion }}
            pathType: Prefix
            {{- end }}
            backend:
              {{- if semverCompare ">=1.14-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}
              servicePort: {{ $svcPort }}
              {{- end }}
          - path: {{ . }}logs
            {{- if semverCompare ">=1.14-0" $.Capabilities.KubeVersion.GitVersion }}
            pathType: Prefix
            {{- end }}
            backend:
              {{- if semverCompare ">=1.14-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-logs
                port:
                  number: {{ $svcLogsPort }}
              {{- else }}
              serviceName: {{ $fullName }}-logs
              servicePort: {{ $svcLogsPort }}
              {{- end -}}
        {{- else }}
          - path: {{ . }}config
            {{- if semverCompare ">=1.14-0" $.Capabilities.KubeVersion.GitVersion }}
            pathType: Prefix
            {{- end }}
            backend:
              {{- if semverCompare ">=1.14-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-config
                port:
                  number: {{ $svcConfigPort }}
              {{- else }}
              serviceName: {{ $fullName }}-config
              servicePort: {{ $svcConfigPort }}
              {{- end -}}
        {{- end }}
        {{- end }}
{{- end }}
{{- end }}
{{- end }}
